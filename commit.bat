@echo off
git commit -m "Schema optimization complete - removed duplicates, added FAQ schema, corrected business hours"
git push origin master
echo Schema optimization committed and pushed successfully!
pause 