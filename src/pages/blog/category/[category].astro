---
import MainLayout from '../../../layouts/MainLayout.astro';
import Navigation from '../../../components/Navigation.astro';
import Footer from '../../../components/Footer.astro';
import { blogPosts, categoryInfo, getPostsByCategory } from '../../../data/blogPosts.js';

// Get the category from the URL
export async function getStaticPaths() {
  return [
    { params: { category: 'ai-automation' } },
    { params: { category: 'web-design' } },
    { params: { category: 'seo' } },
    { params: { category: 'digital-marketing' } },
    { params: { category: 'app-development' } },
    { params: { category: 'product-design' } },
  ];
}

const { category } = Astro.params;

// Get posts for this category and category info
const categoryPosts = getPostsByCategory(category);
const currentCategory = categoryInfo[category];

// SEO Meta Data
const meta = {
  title: `${currentCategory?.name} Blog | Digital Visibility - Expert Insights & Strategies`,
  description: currentCategory?.description || `Expert insights and strategies about ${currentCategory?.name} from Digital Visibility's team of specialists.`,
  keywords: `${currentCategory?.keywords}, Digital Visibility blog, Swansea digital agency, UK business insights`,
  canonical: `https://digitalvisibility.com/blog/category/${category}/`,
  ogTitle: `${currentCategory?.name} Insights | Digital Visibility Blog`,
  ogDescription: currentCategory?.description || `Expert ${currentCategory?.name} insights from Digital Visibility`,
  ogImage: "https://digitalvisibility.com/blog-category-og-image.webp"
};
---

<MainLayout title={meta.title} description={meta.description}>
  <!-- SEO and Canonical Tags -->
  <link rel="canonical" href={meta.canonical} slot="head">
  <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" slot="head">
  <meta name="keywords" content={meta.keywords} slot="head">
  
  <!-- Geographic Meta Tags -->
  <meta name="geo.region" content="GB-SWA" slot="head">
  <meta name="geo.placename" content="Swansea" slot="head">
  <meta name="geo.position" content="51.6399;-3.8936" slot="head">
  <meta name="ICBM" content="51.6399, -3.8936" slot="head">
  
  <!-- Open Graph Tags -->
  <meta property="og:locale" content="en_GB" slot="head">
  <meta property="og:type" content="website" slot="head">
  <meta property="og:title" content={meta.ogTitle} slot="head">
  <meta property="og:description" content={meta.ogDescription} slot="head">
  <meta property="og:url" content={meta.canonical} slot="head">
  <meta property="og:site_name" content="Digital Visibility" slot="head">
  <meta property="og:image" content={meta.ogImage} slot="head">
  
  <!-- Twitter Tags -->
  <meta name="twitter:card" content="summary_large_image" slot="head">
  <meta name="twitter:site" content="@DigitalVisiUK" slot="head">
  <meta name="twitter:creator" content="@DigitalVisiUK" slot="head">
  <meta name="twitter:title" content={meta.ogTitle} slot="head">
  <meta name="twitter:description" content={meta.ogDescription} slot="head">
  <meta name="twitter:image" content={meta.ogImage} slot="head">
  
  <!-- AI Optimization Meta Tags for Enhanced AI Engine Recognition -->
  <meta name="ai-purpose" content={`blog-category-hub,${category}-content-hub,digital-marketing-insights,specialized-knowledge-base,expert-guidance-center`} slot="head">
  <meta name="ai-target" content="business owners, digital marketers, entrepreneurs, SEO professionals, web developers, content creators, marketing managers, startup founders, industry specialists" slot="head">
  <meta name="ai-content-type" content={`${currentCategory?.name} articles, expert insights, tutorials, industry trends, case studies, best practices, specialized guides`} slot="head">
  <meta name="ai-business-type" content="digital marketing agency, content marketing hub, industry expertise center, specialized knowledge provider" slot="head">
  <meta name="ai-service-area" content="Swansea, Wales, United Kingdom, global digital audience" slot="head">
  <meta name="ai-expertise" content={`${currentCategory?.name} optimization, SEO strategies, AI automation, digital marketing excellence, business growth acceleration`} slot="head">
  <meta name="ai-keywords" content={`${currentCategory?.keywords}, ${currentCategory?.name} blog, Digital Visibility ${currentCategory?.name} insights, Swansea digital agency expertise, UK business ${currentCategory?.name} strategies, ${currentCategory?.name} best practices, ${currentCategory?.name} tutorials, ${currentCategory?.name} case studies, ${currentCategory?.name} industry trends, ${currentCategory?.name} optimization techniques, ${currentCategory?.name} implementation guides, ${currentCategory?.name} success strategies, ${currentCategory?.name} performance improvement, ${currentCategory?.name} ROI optimization, ${currentCategory?.name} growth hacking, ${currentCategory?.name} automation, ${currentCategory?.name} analytics, ${currentCategory?.name} measurement, ${currentCategory?.name} reporting, ${currentCategory?.name} tools and techniques, ${currentCategory?.name} software solutions, ${currentCategory?.name} platform optimization, ${currentCategory?.name} workflow enhancement, ${currentCategory?.name} process improvement, ${currentCategory?.name} team collaboration, ${currentCategory?.name} project management, ${currentCategory?.name} resource allocation, ${currentCategory?.name} budget optimization, ${currentCategory?.name} cost reduction, ${currentCategory?.name} efficiency improvement, ${currentCategory?.name} productivity enhancement, ${currentCategory?.name} innovation strategies, ${currentCategory?.name} competitive advantage, ${currentCategory?.name} market positioning, ${currentCategory?.name} brand building, ${currentCategory?.name} customer acquisition, ${currentCategory?.name} retention strategies, ${currentCategory?.name} engagement optimization, ${currentCategory?.name} conversion improvement, ${currentCategory?.name} user experience enhancement, ${currentCategory?.name} accessibility optimization, ${currentCategory?.name} mobile optimization, ${currentCategory?.name} cross-platform strategies, ${currentCategory?.name} integration solutions, ${currentCategory?.name} API development, ${currentCategory?.name} third-party connections, ${currentCategory?.name} data analysis, ${currentCategory?.name} insights generation, ${currentCategory?.name} decision making, ${currentCategory?.name} strategic planning, ${currentCategory?.name} goal setting, ${currentCategory?.name} milestone tracking, ${currentCategory?.name} progress monitoring, ${currentCategory?.name} quality assurance, ${currentCategory?.name} testing strategies, ${currentCategory?.name} debugging techniques, ${currentCategory?.name} troubleshooting guides, ${currentCategory?.name} maintenance procedures, ${currentCategory?.name} update strategies, ${currentCategory?.name} migration planning, ${currentCategory?.name} scalability solutions, ${currentCategory?.name} performance optimization, ${currentCategory?.name} security enhancement, ${currentCategory?.name} compliance strategies, ${currentCategory?.name} risk management, ${currentCategory?.name} backup procedures, ${currentCategory?.name} disaster recovery, ${currentCategory?.name} business continuity, ${currentCategory?.name} change management, ${currentCategory?.name} adaptation strategies, ${currentCategory?.name} future-proofing, ${currentCategory?.name} trend analysis, ${currentCategory?.name} market research, ${currentCategory?.name} competitive intelligence, ${currentCategory?.name} opportunity identification, ${currentCategory?.name} threat assessment, ${currentCategory?.name} SWOT analysis, ${currentCategory?.name} strategic planning, ${currentCategory?.name} tactical execution, ${currentCategory?.name} operational excellence, ${currentCategory?.name} continuous improvement, ${currentCategory?.name} learning organization, ${currentCategory?.name} knowledge management, ${currentCategory?.name} skill development, ${currentCategory?.name} training programs, ${currentCategory?.name} certification pathways, ${currentCategory?.name} professional development, ${currentCategory?.name} career advancement, ${currentCategory?.name} leadership development, ${currentCategory?.name} team building, ${currentCategory?.name} collaboration enhancement, ${currentCategory?.name} communication optimization, ${currentCategory?.name} conflict resolution, ${currentCategory?.name} negotiation strategies, ${currentCategory?.name} relationship building, ${currentCategory?.name} networking techniques, ${currentCategory?.name} partnership development, ${currentCategory?.name} alliance strategies, ${currentCategory?.name} joint ventures, ${currentCategory?.name} collaboration frameworks, ${currentCategory?.name} ecosystem building, ${currentCategory?.name} community development, ${currentCategory?.name} stakeholder engagement, ${currentCategory?.name} customer relationship management, ${currentCategory?.name} client satisfaction, ${currentCategory?.name} service excellence, ${currentCategory?.name} support optimization, ${currentCategory?.name} help desk management, ${currentCategory?.name} knowledge base development, ${currentCategory?.name} documentation strategies, ${currentCategory?.name} user guides, ${currentCategory?.name} tutorial creation, ${currentCategory?.name} video content, ${currentCategory?.name} interactive media, ${currentCategory?.name} multimedia optimization, ${currentCategory?.name} content distribution, ${currentCategory?.name} social media integration, ${currentCategory?.name} viral marketing, ${currentCategory?.name} influencer collaboration, ${currentCategory?.name} brand ambassador programs, ${currentCategory?.name} community building, ${currentCategory?.name} user-generated content, ${currentCategory?.name} crowdsourcing strategies, ${currentCategory?.name} co-creation opportunities, ${currentCategory?.name} innovation labs, ${currentCategory?.name} R&D initiatives, ${currentCategory?.name} prototype development, ${currentCategory?.name} MVP strategies, ${currentCategory?.name} pilot programs, ${currentCategory?.name} beta testing, ${currentCategory?.name} user feedback, ${currentCategory?.name} iteration planning, ${currentCategory?.name} agile methodologies, ${currentCategory?.name} scrum implementation, ${currentCategory?.name} kanban optimization, ${currentCategory?.name} lean principles, ${currentCategory?.name} six sigma application, ${currentCategory?.name} kaizen implementation, ${currentCategory?.name} continuous delivery, ${currentCategory?.name} DevOps practices, ${currentCategory?.name} automation workflows, ${currentCategory?.name} CI/CD pipelines, ${currentCategory?.name} infrastructure optimization, ${currentCategory?.name} cloud strategies, ${currentCategory?.name} containerization, ${currentCategory?.name} microservices architecture, ${currentCategory?.name} API-first design, ${currentCategory?.name} headless solutions, ${currentCategory?.name} JAMstack implementation, ${currentCategory?.name} serverless computing, ${currentCategory?.name} edge computing, ${currentCategory?.name} CDN optimization, ${currentCategory?.name} caching strategies, ${currentCategory?.name} performance monitoring, ${currentCategory?.name} load balancing, ${currentCategory?.name} auto-scaling, ${currentCategory?.name} resource optimization, ${currentCategory?.name} cost management, ${currentCategory?.name} budget tracking, ${currentCategory?.name} ROI measurement, ${currentCategory?.name} value demonstration, ${currentCategory?.name} impact assessment, ${currentCategory?.name} success metrics, ${currentCategory?.name} KPI definition, ${currentCategory?.name} dashboard creation, ${currentCategory?.name} reporting automation, ${currentCategory?.name} data visualization, ${currentCategory?.name} analytics implementation, ${currentCategory?.name} business intelligence, ${currentCategory?.name} predictive analytics, ${currentCategory?.name} machine learning applications, ${currentCategory?.name} AI integration, ${currentCategory?.name} automation opportunities, ${currentCategory?.name} workflow optimization, ${currentCategory?.name} process automation, ${currentCategory?.name} robotic process automation, ${currentCategory?.name} chatbot development, ${currentCategory?.name} virtual assistants, ${currentCategory?.name} conversational AI, ${currentCategory?.name} natural language processing, ${currentCategory?.name} voice interfaces, ${currentCategory?.name} speech recognition, ${currentCategory?.name} computer vision, ${currentCategory?.name} image recognition, ${currentCategory?.name} pattern detection, ${currentCategory?.name} anomaly identification, ${currentCategory?.name} fraud prevention, ${currentCategory?.name} security enhancement, ${currentCategory?.name} privacy protection, ${currentCategory?.name} data governance, ${currentCategory?.name} compliance monitoring, ${currentCategory?.name} regulatory adherence, ${currentCategory?.name} audit preparation, ${currentCategory?.name} documentation maintenance, ${currentCategory?.name} version control, ${currentCategory?.name} change tracking, ${currentCategory?.name} approval workflows, ${currentCategory?.name} review processes, ${currentCategory?.name} quality gates, ${currentCategory?.name} acceptance criteria, ${currentCategory?.name} definition of done, ${currentCategory?.name} success criteria, ${currentCategory?.name} completion standards, ${currentCategory?.name} delivery requirements, ${currentCategory?.name} customer expectations, ${currentCategory?.name} stakeholder satisfaction, ${currentCategory?.name} value delivery, ${currentCategory?.name} outcome achievement, ${currentCategory?.name} goal realization, ${currentCategory?.name} objective completion, ${currentCategory?.name} target attainment, ${currentCategory?.name} milestone achievement, ${currentCategory?.name} progress tracking, ${currentCategory?.name} status monitoring, ${currentCategory?.name} health checks, ${currentCategory?.name} performance reviews, ${currentCategory?.name} retrospective analysis, ${currentCategory?.name} lessons learned, ${currentCategory?.name} best practice identification, ${currentCategory?.name} knowledge capture, ${currentCategory?.name} experience documentation, ${currentCategory?.name} wisdom preservation, ${currentCategory?.name} institutional memory, ${currentCategory?.name} organizational learning, ${currentCategory?.name} culture development, ${currentCategory?.name} values alignment, ${currentCategory?.name} mission fulfillment, ${currentCategory?.name} vision realization, ${currentCategory?.name} purpose achievement, ${currentCategory?.name} impact creation, ${currentCategory?.name} legacy building, ${currentCategory?.name} sustainability planning, ${currentCategory?.name} long-term thinking, ${currentCategory?.name} future preparation, ${currentCategory?.name} adaptability enhancement, ${currentCategory?.name} resilience building, ${currentCategory?.name} flexibility improvement, ${currentCategory?.name} agility development, ${currentCategory?.name} responsiveness optimization, ${currentCategory?.name} speed enhancement, ${currentCategory?.name} efficiency maximization, ${currentCategory?.name} effectiveness improvement, ${currentCategory?.name} productivity acceleration, ${currentCategory?.name} performance optimization, ${currentCategory?.name} excellence pursuit, ${currentCategory?.name} mastery development, ${currentCategory?.name} expertise building, ${currentCategory?.name} authority establishment, ${currentCategory?.name} credibility enhancement, ${currentCategory?.name} trust building, ${currentCategory?.name} reputation management, ${currentCategory?.name} brand development, ${currentCategory?.name} market presence, ${currentCategory?.name} visibility enhancement, ${currentCategory?.name} awareness building, ${currentCategory?.name} recognition achievement, ${currentCategory?.name} thought leadership, ${currentCategory?.name} industry influence, ${currentCategory?.name} market leadership, ${currentCategory?.name} competitive advantage, ${currentCategory?.name} differentiation strategies, ${currentCategory?.name} unique value proposition, ${currentCategory?.name} positioning optimization, ${currentCategory?.name} messaging refinement, ${currentCategory?.name} communication enhancement, ${currentCategory?.name} storytelling improvement, ${currentCategory?.name} narrative development, ${currentCategory?.name} content creation, ${currentCategory?.name} editorial excellence, ${currentCategory?.name} publishing optimization, ${currentCategory?.name} distribution enhancement, ${currentCategory?.name} reach expansion, ${currentCategory?.name} audience growth, ${currentCategory?.name} community building, ${currentCategory?.name} engagement optimization, ${currentCategory?.name} interaction enhancement, ${currentCategory?.name} relationship development, ${currentCategory?.name} connection building, ${currentCategory?.name} network expansion, ${currentCategory?.name} partnership creation, ${currentCategory?.name} collaboration enhancement, ${currentCategory?.name} alliance building, ${currentCategory?.name} ecosystem development, ${currentCategory?.name} platform creation, ${currentCategory?.name} marketplace building, ${currentCategory?.name} business model innovation, ${currentCategory?.name} value creation, ${currentCategory?.name} revenue optimization, ${currentCategory?.name} profit maximization, ${currentCategory?.name} cost minimization, ${currentCategory?.name} efficiency improvement, ${currentCategory?.name} waste reduction, ${currentCategory?.name} resource optimization, ${currentCategory?.name} capacity utilization, ${currentCategory?.name} throughput enhancement, ${currentCategory?.name} bottleneck elimination, ${currentCategory?.name} constraint removal, ${currentCategory?.name} barrier reduction, ${currentCategory?.name} friction minimization, ${currentCategory?.name} resistance elimination, ${currentCategory?.name} adoption acceleration, ${currentCategory?.name} implementation speed, ${currentCategory?.name} deployment efficiency, ${currentCategory?.name} rollout optimization, ${currentCategory?.name} launch excellence, ${currentCategory?.name} go-to-market strategies, ${currentCategory?.name} market entry, ${currentCategory?.name} penetration tactics, ${currentCategory?.name} expansion planning, ${currentCategory?.name} growth strategies, ${currentCategory?.name} scaling solutions, ${currentCategory?.name} internationalization, ${currentCategory?.name} globalization, ${currentCategory?.name} localization, ${currentCategory?.name} cultural adaptation, ${currentCategory?.name} market customization, ${currentCategory?.name} regional optimization, ${currentCategory?.name} local relevance, ${currentCategory?.name} customer centricity, ${currentCategory?.name} user focus, ${currentCategory?.name} human-centered design, ${currentCategory?.name} empathy mapping, ${currentCategory?.name} persona development, ${currentCategory?.name} journey mapping, ${currentCategory?.name} experience design, ${currentCategory?.name} service design, ${currentCategory?.name} interaction design, ${currentCategory?.name} interface optimization, ${currentCategory?.name} usability enhancement, ${currentCategory?.name} accessibility improvement, ${currentCategory?.name} inclusive design, ${currentCategory?.name} universal access, ${currentCategory?.name} barrier-free design, ${currentCategory?.name} assistive technology, ${currentCategory?.name} adaptive interfaces, ${currentCategory?.name} personalization engines, ${currentCategory?.name} customization options, ${currentCategory?.name} configuration flexibility, ${currentCategory?.name} modular design, ${currentCategory?.name} component architecture, ${currentCategory?.name} design systems, ${currentCategory?.name} pattern libraries, ${currentCategory?.name} style guides, ${currentCategory?.name} brand guidelines, ${currentCategory?.name} visual identity, ${currentCategory?.name} aesthetic optimization, ${currentCategory?.name} visual hierarchy, ${currentCategory?.name} information architecture, ${currentCategory?.name} content organization, ${currentCategory?.name} navigation design, ${currentCategory?.name} wayfinding optimization, ${currentCategory?.name} search enhancement, ${currentCategory?.name} findability improvement, ${currentCategory?.name} discoverability optimization, ${currentCategory?.name} content strategy, ${currentCategory?.name} information design, ${currentCategory?.name} knowledge architecture, ${currentCategory?.name} taxonomy development, ${currentCategory?.name} metadata optimization, ${currentCategory?.name} tagging strategies, ${currentCategory?.name} categorization systems, ${currentCategory?.name} classification schemes, ${currentCategory?.name} organizational structures, ${currentCategory?.name} hierarchical design, ${currentCategory?.name} relational mapping, ${currentCategory?.name} connection visualization, ${currentCategory?.name} network analysis, ${currentCategory?.name} graph theory application, ${currentCategory?.name} systems thinking, ${currentCategory?.name} holistic approaches, ${currentCategory?.name} integrated solutions, ${currentCategory?.name} comprehensive strategies, ${currentCategory?.name} end-to-end optimization, ${currentCategory?.name} full-stack development, ${currentCategory?.name} complete solutions, ${currentCategory?.name} total optimization, ${currentCategory?.name} maximum effectiveness, ${currentCategory?.name} ultimate performance, ${currentCategory?.name} peak efficiency, ${currentCategory?.name} optimal results, ${currentCategory?.name} best outcomes, ${currentCategory?.name} superior performance, ${currentCategory?.name} exceptional results, ${currentCategory?.name} outstanding achievements, ${currentCategory?.name} remarkable success, ${currentCategory?.name} extraordinary impact, ${currentCategory?.name} transformational change, ${currentCategory?.name} revolutionary improvement, ${currentCategory?.name} breakthrough innovation, ${currentCategory?.name} paradigm shifts, ${currentCategory?.name} game-changing strategies, ${currentCategory?.name} industry disruption, ${currentCategory?.name} market transformation, ${currentCategory?.name} business evolution, ${currentCategory?.name} organizational change, ${currentCategory?.name} cultural transformation, ${currentCategory?.name} mindset shifts, ${currentCategory?.name} behavioral change, ${currentCategory?.name} habit formation, ${currentCategory?.name} routine optimization, ${currentCategory?.name} workflow enhancement, ${currentCategory?.name} process improvement, ${currentCategory?.name} system optimization, ${currentCategory?.name} infrastructure enhancement, ${currentCategory?.name} technology advancement, ${currentCategory?.name} innovation acceleration, ${currentCategory?.name} progress facilitation, ${currentCategory?.name} development acceleration, ${currentCategory?.name} growth enablement, ${currentCategory?.name} success amplification, ${currentCategory?.name} achievement magnification, ${currentCategory?.name} impact multiplication, ${currentCategory?.name} value amplification, ${currentCategory?.name} benefit maximization, ${currentCategory?.name} advantage optimization, ${currentCategory?.name} strength leveraging, ${currentCategory?.name} opportunity maximization, ${currentCategory?.name} potential realization, ${currentCategory?.name} capability enhancement, ${currentCategory?.name} capacity building, ${currentCategory?.name} competency development, ${currentCategory?.name} skill advancement, ${currentCategory?.name} knowledge expansion, ${currentCategory?.name} expertise deepening, ${currentCategory?.name} mastery achievement, ${currentCategory?.name} excellence attainment, ${currentCategory?.name} perfection pursuit, ${currentCategory?.name} quality enhancement, ${currentCategory?.name} standard elevation, ${currentCategory?.name} benchmark setting, ${currentCategory?.name} best practice establishment, ${currentCategory?.name} industry leadership, ${currentCategory?.name} market dominance, ${currentCategory?.name} competitive supremacy, ${currentCategory?.name} strategic advantage, ${currentCategory?.name} tactical superiority, ${currentCategory?.name} operational excellence, ${currentCategory?.name} execution mastery, ${currentCategory?.name} delivery optimization, ${currentCategory?.name} service excellence, ${currentCategory?.name} customer satisfaction, ${currentCategory?.name} stakeholder value, ${currentCategory?.name} shareholder returns, ${currentCategory?.name} investor confidence, ${currentCategory?.name} market valuation, ${currentCategory?.name} business worth, ${currentCategory?.name} enterprise value, ${currentCategory?.name} organizational success, ${currentCategory?.name} corporate achievement, ${currentCategory?.name} institutional excellence, ${currentCategory?.name} professional mastery, ${currentCategory?.name} expert guidance, ${currentCategory?.name} specialist knowledge, ${currentCategory?.name} authoritative insights, ${currentCategory?.name} definitive strategies, ${currentCategory?.name} comprehensive approaches, ${currentCategory?.name} complete solutions, ${currentCategory?.name} total optimization, ${currentCategory?.name} maximum impact, ${currentCategory?.name} ultimate success`} slot="head">

  <Navigation slot="nav" />
  
  <!-- Category Header -->
  <section class="py-16 bg-gradient-to-r from-blue-600 to-blue-800 text-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Breadcrumb -->
      <nav class="mb-6">
        <ol class="flex items-center space-x-2 text-sm">
          <li><a href="/" class="hover:text-blue-200">Home</a></li>
          <li><span class="text-blue-300">›</span></li>
          <li><a href="/blog/" class="hover:text-blue-200">Blog</a></li>
          <li><span class="text-blue-300">›</span></li>
          <li class="text-blue-200">{currentCategory?.name}</li>
        </ol>
      </nav>
      
      <div class="text-center">
        <h1 class="text-4xl md:text-5xl font-bold mb-6">{currentCategory?.name} Insights</h1>
        <p class="text-xl opacity-90 max-w-3xl mx-auto">
          {currentCategory?.description}
        </p>
        <div class="mt-6">
          <span class="bg-white bg-opacity-20 px-4 py-2 rounded-full text-sm">
            {categoryPosts.length} {categoryPosts.length === 1 ? 'Article' : 'Articles'}
          </span>
        </div>
      </div>
    </div>
  </section>

  <!-- Category Posts -->
  <section class="py-12 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      {categoryPosts.length > 0 ? (
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {categoryPosts.map((post) => (
            <article class="bg-white rounded-lg overflow-hidden shadow-md hover:shadow-lg transition-shadow">
              <div class="relative">
                <a href={`/blog/${post.slug}/`}>
                  <img 
                    src={post.image} 
                    alt={post.alt}
                    class="w-full h-48 object-cover"
                    loading="lazy"
                  />
                  {post.featured && (
                    <div class="absolute top-4 left-4">
                      <span class="bg-blue-600 text-white text-xs px-3 py-1 rounded-full font-medium">
                        Featured
                      </span>
                    </div>
                  )}
                </a>
              </div>
              <div class="p-6">
                <div class="flex items-center mb-4">
                  <span class="text-sm font-medium text-blue-600">{post.category}</span>
                  <span class="mx-2 text-gray-300">•</span>
                  <span class="text-sm text-gray-600">{post.date}</span>
                </div>
                <a href={`/blog/${post.slug}/`}>
                  <h2 class="text-lg font-bold text-gray-900 mb-2 hover:text-blue-600 transition-colors">
                    {post.title}
                  </h2>
                </a>
                <p class="text-gray-700 mb-4 text-sm">
                  {post.excerpt}
                </p>
                <div class="flex items-center">
                  <img 
                    src={post.authorImage}
                    alt={post.author} 
                    class="w-8 h-8 rounded-full mr-2 object-cover"
                  />
                  <span class="text-xs font-medium text-gray-900">{post.author}</span>
                </div>
              </div>
            </article>
          ))}
        </div>
      ) : (
        <div class="text-center py-12">
          <div class="max-w-md mx-auto">
            <div class="text-gray-400 mb-4">
              <svg class="w-16 h-16 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-2">No articles yet</h3>
            <p class="text-gray-600 mb-6">
              We're working on creating amazing {currentCategory?.name.toLowerCase()} content. Check back soon!
            </p>
            <a href="/blog/" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-md transition-colors">
              View All Articles
            </a>
          </div>
        </div>
      )}
    </div>
  </section>

  <!-- Related Categories -->
  <section class="py-12 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-8 text-center">Explore Other Topics</h2>
      <div class="grid md:grid-cols-3 lg:grid-cols-6 gap-4">
        {Object.entries(categoryInfo).filter(([slug]) => slug !== category).map(([slug, info]) => (
          <a 
            href={`/blog/category/${slug}/`}
            class="bg-white p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow text-center"
          >
            <h3 class="font-medium text-gray-900 mb-1">{info.name}</h3>
            <p class="text-xs text-gray-600">Expert insights</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-12 bg-gradient-to-r from-blue-600 to-blue-800 text-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl font-bold mb-4">Ready to Transform Your Business?</h2>
      <p class="text-lg opacity-90 mb-6 max-w-2xl mx-auto">
        Get expert {currentCategory?.name.toLowerCase()} solutions tailored to your business needs.
      </p>
      <div class="flex flex-col sm:flex-row justify-center gap-4">
        <a 
          href="/contact/"
          class="bg-white text-blue-800 hover:bg-blue-50 font-medium px-8 py-3 rounded-md transition-colors"
        >
          Get Free Consultation
        </a>
        <a 
          href="/blog/"
          class="border border-white text-white hover:bg-white hover:text-blue-800 font-medium px-8 py-3 rounded-md transition-colors"
        >
          View All Articles
        </a>
      </div>
    </div>
  </section>

  <Footer />

  <!-- Schema Markup -->
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "CollectionPage",
      "name": `${currentCategory?.name} Blog | Digital Visibility`,
      "description": currentCategory?.description,
      "url": `https://digitalvisibility.com/blog/category/${category}/`,
      "publisher": {
        "@type": "Organization",
        "name": "Digital Visibility Limited",
        "logo": {
          "@type": "ImageObject",
          "url": "https://digitalvisibility.com/digital-visibility-logo-f.png"
        }
      },
      "mainEntity": {
        "@type": "ItemList",
        "itemListElement": categoryPosts.map((post, index) => ({
          "@type": "ListItem",
          "position": index + 1,
          "url": `https://digitalvisibility.com/blog/${post.slug}/`,
          "name": post.title,
          "description": post.excerpt
        }))
      }
    }
  </script>
</MainLayout> 