---
// Middleware Test Endpoint
// Returns a simple text response to verify the deployment is live

export const prerender = false; // Must be SSR

// Return plain text response
return new Response('✓ Middleware is working! Deployment successful at ' + new Date().toISOString(), {
  status: 200,
  headers: {
    'Content-Type': 'text/plain',
    'X-Deployment-Test': 'true',
    'X-Powered-By': 'Astro SSR on Cloudflare Pages'
  }
});
---
